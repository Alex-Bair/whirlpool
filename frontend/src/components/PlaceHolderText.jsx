import { Box, Typography } from '@mui/material';

const PlaceHolderText = () => {
  return (
    <Box>
      <Typography>
        Send requests to the endpoint above, and you can inspect them after clicking on a specific request.
      </Typography>
    </Box>
  );
};

export default PlaceHolderText;
